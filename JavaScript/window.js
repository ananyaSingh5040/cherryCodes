//Window objects, DOM and BOM:

window.console.log(window);
//a global object is window.
//window has dom , bom and core javascript.
//dom is a javacript object which is your html only.
//bom are additional objects provided by the host environment like alert, confirm, prompt , location.href etc.

console.log(document);
// document.body.style.background="yellow"; use it in browser.